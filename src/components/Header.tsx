type HeaderProps = {
  id: number | null;
  setId: (id: number) => void;
  name: string | null;
  setName: (name: string) => void;
};

const Header = ({id, setId, name, setName}: HeaderProps) => null;
export default Header;
